name: testing-back

on: pull_request

jobs:
  test-back:
    runs-on: ubuntu-lastest
    steps:
      - name: Check out Code
        uses: actions/checkout@v2
      - name: Run test
        run: yarn test:ci
